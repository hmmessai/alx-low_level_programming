#include "main.h"
/**
 *main - outputs the name of the program run to get this output
 *@argc: the argument count
 *@argv: the array of the arguments used
 *Return: the name of the program that run
 */
int main(int argc, char *argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
