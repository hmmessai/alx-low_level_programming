#include "main.h"
/**
 *add - adds two integers
 *@a: the first addend
 *@b: the second addend
 *Return: sum(the sum of the two integers)
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
