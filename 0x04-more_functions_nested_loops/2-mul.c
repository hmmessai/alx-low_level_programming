#include "main.h"
/**
 *mul - multiplies two digits
 *@a: the first number to be multiplied
 *@b: the second number to be multiplied
 *Return: product
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
